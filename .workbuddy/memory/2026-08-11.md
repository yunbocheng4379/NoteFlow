# 2026-08-11 工作日志

## NoteFlow 微信小程序架构设计 + 完整开发

### 架构设计（上午）
- 完成 12 章架构设计文档，保存在 docs/miniprogram-architecture.md
- 输出 2 张可视化架构图 + 核心用户流程图
- 分阶段上线计划：Phase 1(MVP) → Phase 2(支付+AI问答) → Phase 3(合集+知识库+闪记卡) → Phase 4(社交裂变)

### 小程序核心分析
- NoteFlow 是 AI 视频笔记系统：视频链接 → 下载 → 转写 → LLM → Markdown 笔记
- 后端 FastAPI (port 8483)，API 前缀 /api
- 计费系统：电力积分(credits) + 会员订阅 + 充值套餐

### 小程序完整开发（119 个文件）

**基础设施（44 文件）**
- 6 项目配置：app.js/json/wxss, project.config.json, sitemap.json, .env.js
- 7 工具模块：request(401重试)、auth(微信登录桥接)、task-polling、platform-detector、markdown-parser、format、event-bus
- 10 API 服务层 + 1 状态管理
- 8 自定义组件：task-card/video-preview/markdown-viewer/chat-bubble/credit-badge/empty-state/loading-skeleton/model-selector

**主包页面（20 文件）**
- home：URL解析 + 模型选择 + 轮询进度
- tasks：下拉刷新 + 分页 + 长按删除
- profile：积分展示 + 功能菜单
- note-detail：Markdown + AI问答Tab
- login：微信一键登录

**5 个子分包（36 文件）**
- billing（12）：recharge(积分套餐选择+微信支付)、subscription(月/季/年方案对比)、orders(订单列表+分页)
- collections（8）：list(合集网格+FAB新建)、detail(笔记列表+重命名+移除)
- knowledge-base（4）：chat(全笔记RAG问答+流式显示+建议问题)
- flashcards（8）：generate(选笔记→AI生成→翻转预览→保存)、study(复习模式+翻转+自评掌握度+统计)
- share（4）：view(分享落地页+内容预览+引导进入小程序)

**资源**
- 6 TabBar PNG 图标 + 图标生成脚本

### 微信公众平台配置
- **APPID**: wxc7bd21a33355b95f（已写入 project.config.json）
- API 域名：需在公众平台「开发管理 → 服务器域名」配置 request 合法域名
- 隐私接口：需在小程序后台声明「用户信息获取」等隐私接口

### 小程序 Bug 修复（下午）
| 问题 | 根因 | 修复 |
|---|---|---|
| login is not a function | auth.js 导出 wechatLogin，非 login | 改 import + 方法名 |
| getPhoneNumber 权限报错 | 按钮设了 open-type=getPhoneNumber | 去掉，改用普通 tap |
| modelApi.getProviders undefined | model.js 导出结构不匹配 | 统一为 { modelApi: { getProviders } } |
| WXSS 标签选择器警告 | 组件中用了 .tab-active text 等 | 改为 class 选择器 .tab-active-text |
| /api/auth/wechat-login 404 | 后端未实现 | 新增完整接口 |

### 后端 wechat-login 接口（下午）
- 新增 `POST /api/auth/wechat-login`：code → code2session → openid → 查/建用户 → JWT
- User 模型新增 wechat_openid、wechat_unionid 字段
- email/hashed_password 改为 nullable（微信用户无需邮箱注册）
- MySQL 已执行 ALTER TABLE 迁移
- .env 新增 WECHAT_MP_APPID + WECHAT_MP_SECRET
- 接口已通过 curl 测试验证返回（AppSecret 占位符，需替换为真实值）

### 后端待改造
- ~~/api/auth/wechat-login 接口~~ ✅ 已完成
- WECHAT_MP 支付渠道（wx.requestPayment 对接）
